1.  Автотест подгружает в файл все библиотеки, даже если в файле они не используются.
В ручном тесте я подгружаю только те библиотеки, которые мне нужны.

2. В автотесте использует класс.
В ручном тесте использовала функции.

3. В автотесте открытие/закрытие браузера в двух отдельных методах класса
В ручном тесте эти операции вынесены в фикстуру файла конфигурации и разделены служебным словом  yeld

4. В автотесте использована команда закрытия драйвера.
В ручном использую только команду quit()

5. В автотесте устанавливается размер открываемого окна.
В ручном используется умолчание.

6. В автотесте для поиска элементов используется исключительно библиотека selenium.webdriver.common.by 
В ручном тесте использую разные функции поиска библиотеки webdriver 

7. В автотесте не используется функция Wait.
Использую функцию Wait

8. Автотест плохо читабелен, все строки идут подряд.
В ручном стараюсь соблюдать принципы удобства чтения.

Вообще , автотест очень удобен, чтобы не копаться в коде странички для поиска идентификаторов, он сильно экономит время на отладку. Но я бы дорабатывала его вручную.